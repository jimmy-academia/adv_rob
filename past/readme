proto = previous try... prototypical ideas
init = first try
sys = second try... systematic parameter search, hacking code (works)
mod = third try... try to modularize network components but failed (backprop cannot work)
overfit = fourth try ... patch tokenizer overfits, need regularization...
standalone = standalone patch dataset to train large vocab_size
0605 = src at 0605 (might be duplicate as mnist_good)

=====

mnist_good
    
{
    obtain 90+ in mnist
    
}